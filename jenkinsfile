pipeline {
    agent any

    // stages {
      
    //     stage('Check Python') {
    //         steps {
    //             sh '''
    //             python api.py
    //             '''
    //         }
    //     }
    // }

    stages {
      
        stage('Test') {
            steps {
                sh '''
                docker exec -u root python-runner bash -c "python api.py"
                '''
            }
        }
    }

    post {
        always {
            echo 'Pipeline finished.'
        }
        success {
            echo 'Pipeline successful!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
